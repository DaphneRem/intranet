import { FichesMaterielLibModule } from './fiches-materiel-lib.module';

describe('FichesMaterielLibModule', () => {
  it('should work', () => {
    expect(new FichesMaterielLibModule()).toBeDefined();
  });
});
