import { DiffusionsDatesModule } from './diffusions-dates.module';

describe('DatesDiffusionsModule', () => {
  it('should work', () => {
    expect(new (DiffusionsDatesModule)).toBeDefined();
  });
});
