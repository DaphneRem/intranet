import { CustomDatatablesModule } from './custom-datatables.module';

describe('CustomDatatablesModule', () => {
  it('should work', () => {
    expect(new CustomDatatablesModule()).toBeDefined();
  });
});
